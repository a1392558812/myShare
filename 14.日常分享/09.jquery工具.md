```html
<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>jquery工具</title>
		<script src="js/jquery-1.12.4.js" type="text/javascript" charset="utf-8"></script>
		<script type="text/javascript">
			$(function(){
			/*
				1.$.each(obj)遍历数组或者对象中的数据
				2.$.type(obj)得到数据的类型
				3.$.isArray(obj)判断是否为数组
				4.$.isfunction(fun)判断是否为函数
				5.$.trim(str)去除字符串两边空格
				6.$.parseJSON(obj)将JSON字符串转换为JS对象/数组
				7.$.
			*/
			})
		</script>
	</head>
	<body>
		
	</body>
</html>

```