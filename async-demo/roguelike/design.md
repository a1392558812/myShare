# Roguelike 随机性与 Boss 机制：设计与改版思路

## 一、现状诊断

| 维度 | 现状 | 问题 |
|------|------|------|
| **敌人种类** | 仅 3 种（近战/远程/混合），行为模式固定 | 玩 5 分钟就体验完了所有内容 |
| **敌人个体** | 同类型完全一致，无变体 | 没有"精英怪"的惊喜/惊吓感 |
| **Boss 战** | 无 | 缺少高潮节点和节奏变化 |
| **环境** | 空旷无限平面 | 没有地形交互、随机事件 |
| **掉落** | 仅血瓶+金币 | 没有功能性道具/临时强化 |
| **节奏** | 平滑刷怪→索引难度 | 缺少"爆发点" |

---

## 二、改版总体架构：四层随机性模型

```
┌─────────────────────────────────────────────────────┐
│                   Layer 4: Boss 波次                  │
│  定时触发 → 全屏警告 → 多阶段 Boss 战 → 特殊掉落       │
├─────────────────────────────────────────────────────┤
│                   Layer 3: 随机事件                   │
│  场上概率出现 → 祭坛/宝箱/诅咒石碑 → 玩家抉择          │
├─────────────────────────────────────────────────────┤
│                   Layer 2: 精英词缀                   │
│  敌人诞生时 X% 概率携带词缀 → 外观+能力双异化          │
├─────────────────────────────────────────────────────┤
│                   Layer 1: 敌人多样性                  │
│  扩充类型表 + 同类型属性浮动 (±20%)                   │
└─────────────────────────────────────────────────────┘
```

---

## 三、Layer 1 — 敌人多样性增强（改动量：小）

### 3.1 同类型属性浮动

在 `useEnemySpawner.spawnEnemy()` 中，生成敌人属性时加 ±15%~20% 的随机浮动：

```
hp    = scaledHp   * random(0.85, 1.15)
speed = attrs.speed * random(0.9, 1.1)
size  = attrs.size  * random(0.9, 1.2)  // 大小微变，视觉差异
```

这样即使同一类型敌人，每次遭遇也略有不同，不会出现"两只远程怪行为完全一致"的无聊感。

### 3.2 扩充敌人类型（建议新增 3~4 种）

| 类型 | 特点 | 定位 |
|------|------|------|
| **自爆怪** `bomber` | 低血量高速度，接近玩家后自爆造成 AoE 伤害 | 压迫走位 |
| **召唤师** `summoner` | 远程，不直接攻击，定期召小怪 | 优先击杀目标 |
| **冲锋者** `charger` | 中血量，周期性直线冲锋（短时间超高速），撞到玩家造成大量伤害+击退 | 考验反应 |
| **护盾兵** `shielder` | 跟随最近的敌人，给它提供减伤光环 | 改变击杀顺序 |

每个新类型需要在 `constants.js` 的 `ENEMY_TYPE_TABLE` 注册，并在 `useEnemy.js` 的 `updateEnemies` 中添加对应 AI 逻辑。

---

## 四、Layer 2 — 精英词缀系统（改动量：中）⭐ 核心

这是**性价比最高的改版**——改动量适中，但对游戏体验提升极大。

### 4.1 词缀定义

在 `constants.js` 新增 `ENEMY_AFFIX_TABLE`：

| 词缀 ID | 名称 | 效果 | 稀有度 | 视觉标识 |
|---------|------|------|--------|----------|
| `fast` | 疾风 | 移速 +40% | 普通 | 青色尾迹 |
| `giant` | 巨化 | 体型 1.5x，HP +50%，伤害 +30% | 普通 | 明显更大 |
| `splitting` | 分裂 | 死亡时分裂为 2 只小型版（HP 30%） | 稀有 | 身体闪烁 |
| `explosive` | 爆裂 | 死亡时产生 AoE 爆炸（伤害玩家和周围敌人） | 稀有 | 红色脉冲光 |
| `shielded` | 护盾 | 带 30% HP 的护盾条，破盾前本体减伤 50% | 稀有 | 蓝色光罩 |
| `vampiric` | 吸血 | 攻击回复自身 HP（伤害的 30%） | 稀有 | 血色光环 |
| `frost` | 冰霜 | 攻击附带减速效果，死亡释放冰冻 nova | 稀有 | 冰蓝粒子 |
| `summoning` | 召唤 | 首次受伤时在场边召唤 2 只普通怪 | 稀有 | 紫色符文环绕 |
| `immortal` | 不朽 | 第一次死亡后复活（HP 50%） | 史诗 | 金色光环 |
| `mirror` | 镜像 | 周期性制造 1 个幻影分身（HP 10%，无碰撞伤害） | 史诗 | 半透明闪烁 |

### 4.2 词缀生效时机

**在 `spawnEnemy()` 最后一步**，按概率判定是否附加词缀：

```
普通怪： 15% 概率携带 1 个普通词缀
稀有怪： 5% 概率携带 1 个稀有词缀（或 1 普通 + 1 稀有）
史诗怪： 1% 概率携带 1 个史诗词缀（叠加在稀有之上）
```

被附魔的敌人：
- 会显示特殊前缀名（如"巨化 近战敌人"）
- 会渲染特殊的视觉特效（光环/粒子/尺寸变化）
- 掉落率翻倍 / 额外掉落
- 经验奖励 ×1.5~3

### 4.3 数据结构

敌人对象新增字段：

```js
{
  // ...现有字段...
  affixes: [],           // [{ id: 'giant', tier: 'common' }, ...]
  shield: 0,             // 护盾值（shielded 词缀）
  reviveCount: 0,        // 已复活次数（immortal 词缀）
}
```

### 4.4 实现位置

- **词缀定义**：`constants.js` 新增 `ENEMY_AFFIX_TABLE`
- **词缀附加**：`useEnemySpawner.spawnEnemy()` 末尾
- **词缀效果**：`useEnemy.js` 的 `updateEnemies()` 中注入对应逻辑
- **词缀渲染**：`GameCanvas.vue` 的敌人绘制部分
- **死亡特效**：`usePlayer.damageEnemy()` 检查词缀触发

---

## 五、Layer 3 — 随机事件与环境交互（改动量：中）

### 5.1 事件类型

| 事件 | 触发方式 | 效果 | 持续时间 |
|------|----------|------|----------|
| 🗿 **力量祭坛** | 玩家靠近按 F | 攻击力 +50% | 30s |
| 🔵 **速度神龛** | 玩家靠近按 F | 移速 +40%，闪避 20% | 20s |
| 📦 **宝箱** | 自动拾取 | 随机掉落 2-3 件道具 | 永久 |
| ⚠️ **诅咒石碑** | 玩家选择是否激活 | 敌人变强 30% 但掉落率×3 | 60s（高风险高回报） |
| 💀 **死亡区域** | 进入即生效 | 持续扣血 / 减速 | 区域内永久 |

### 5.2 生成规则

- 每 30~60 秒概率在地图随机位置生成 1 个事件
- 场上最多同时存在 3 个事件
- 祭坛/神龛有使用冷却（同类型 60s 内不重复生成）
- 事件出现在屏幕内任意位置（非边界处）

### 5.3 数据结构

```js
// 新状态文件
const worldEvents = ref([])
// 每个事件:
{
  id: 'shrine_power',
  type: 'shrine',        // shrine | chest | curse | zone
  name: '力量祭坛',
  icon: '🗿',
  x, y,
  radius: 40,
  effect: { ... },
  duration: 30000,
  elapsed: 0,
  used: false,           // 是否已激活（避免重复）
}
```

### 5.4 新增文件

建议新增 `composables/useWorldEvents.js`，负责事件的生成、生命周期、效果应用。

---

## 六、Layer 4 — Boss 波次系统（改动量：大）⭐⭐ 高光点

### 6.1 触发条件

不按"杀敌数"触发（那样会变成纯数值游戏），而是按 **游戏时间 + 玩家等级混合** 触发：

| Boss | 触发条件 | 冷却 |
|------|----------|------|
| **第 1 只 Boss** | 游戏时间 ≥ 2 分钟 + 玩家 Lv ≥ 3 | — |
| **后续 Boss** | 距上次 Boss 击败 ≥ 3 分钟 + 玩家 Lv 高于上次 +2 | 3min |

这样确保玩家有成长空间，同时保持节奏。

### 6.2 Boss 设计（建议 3 只，逐步解锁）

#### Boss 1: **暗影聚合体** `shadow amalgam`

```
HP: 300 + 30×(玩家等级-3)
体型: 3x 普通敌人
移动: 缓慢追踪 (speed 1.5)
```

**阶段 1（HP > 60%）**：
- 近战拍击（扇形 AoE，150px 范围）
- 发射 3 枚慢速追踪暗影球（伤害 15）

**阶段 2（HP ≤ 60%）**：
- 新增"暗影分身"：分裂为 3 个幻影，随机位置闪现，只有真身可受伤（真身有微弱金色轮廓）
- 暗影球数量增加到 5 枚

**阶段 3（HP ≤ 25%）**：
- 狂暴：移速翻倍，攻击冷却减半
- 地板出现周期性暗影波纹（需走位躲避）

#### Boss 2: **火焰核心** `inferno core`（解锁条件：玩家 Lv ≥ 8）

```
HP: 600 + 50×(玩家等级-8)
体型: 2.5x
移动: 中速 (speed 2.2)，但倾向于保持距离
```

**核心机制**：不直接追击，而是在场地中周期性召唤火柱（地面预警圈→0.8s 后喷发）。同时释放旋转火焰弹幕（8 方向）。

#### Boss 3: **虚空织法者** `void weaver`（解锁条件：玩家 Lv ≥ 12）

```
HP: 1000 + 80×(玩家等级-12)
体型: 2x
移动: 传送 (每 3s 瞬移到场地随机位置)
```

**核心机制**：全场布局——布置"虚空线"，两条虚空线之间的连线会造成持续伤害。Boss 同时释放减速场和追踪弹幕。玩家需要在有限的安全区域内战斗。

### 6.3 Boss 战流程

```
1. 触发条件满足
2. 全屏警告文字 "⚠️ 强大的敌人正在接近..."
3. 屏幕边缘变红脉冲 2 秒
4. 暂停普通刷新 5 秒
5. Boss 从屏幕中央诞生（有登场动画）
6. Boss 血条显示在屏幕顶部（独立 UI）
7. 战斗中普通刷新频率降低 50%
8. Boss 击败 → 大爆炸特效 + 掉落专属道具
9. 恢复普通刷新
```

### 6.4 Boss 数据结构

```js
// constants.js 新增 BOSS_TABLE
const BOSS_TABLE = [
  {
    id: 'shadowAmalgam',
    name: '暗影聚合体',
    unlockPlayerLevel: 3,
    hp: 300,
    hpPerLevel: 30,
    speed: 1.5,
    size: ENTITY_SIZE * 3,
    color: '#4b0082',
    color2: '#1a0033',
    phases: [
      { hpThreshold: 1.0,  attacks: ['meleeSlam', 'shadowOrbs'] },
      { hpThreshold: 0.6,  attacks: ['meleeSlam', 'shadowOrbs', 'shadowClone'] },
      { hpThreshold: 0.25, attacks: ['meleeSlam', 'shadowOrbs', 'shadowClone', 'frenzy', 'shadowWave'] },
    ],
    dropTable: [ ... ],
    expReward: 200,
  },
  // ...更多 Boss
]
```

### 6.5 Boss 掉落设计

| Boss | 专属掉落 | 效果 |
|------|----------|------|
| 暗影聚合体 | 暗影碎片 | 技能冷却 -15%（被动，永久） |
| 火焰核心 | 火种 | 所有火焰伤害 +25%（被动，永久） |
| 虚空织法者 | 虚空之眼 | 获得 1 次"闪避"充能（致命伤时免疫并瞬移，45s 冷却） |

---

## 七、实现路线图

```
Phase 1（改动量小，1-2h）
├── 同类型属性浮动 (±15%)
└── 新增 2 个敌人类型（自爆怪 + 冲锋者）

Phase 2（改动量中，3-4h）⭐ 优先
├── 精英词缀系统（先做 5 个核心词缀）
│   ├── 巨化、疾风、分裂、爆裂、不朽
│   └── 附带渲染特效
└── 词缀附加逻辑 + 掉落加成

Phase 3（改动量中，2-3h）
├── 随机事件系统
│   ├── 祭坛、宝箱、诅咒石碑
│   └── 交互按键 + 效果应用
└── 死亡区域

Phase 4（改动量大，5-8h）
├── Boss 波次系统
│   ├── Boss 1：暗影聚合体（3 阶段）
│   ├── Boss 2：火焰核心（火柱+旋转弹幕）
│   └── Boss 3：虚空织法者（传送+场地控制）
├── Boss 血条 UI
├── 多阶段 AI
└── 专属掉落
```

---

## 八、关键技术要点

### 8.1 词缀系统与现有逻辑的集成

词缀主要在三个地方生效：
1. **属性修改**（spawnEnemy 时）：直接乘到 hp/speed/attack 上
2. **行为注入**（updateEnemies 时）：检查 `affixes` 数组决定额外行为
3. **死亡触发**（damageEnemy 时）：检查 `affixes` + 敌人 `dead === true` 时触发 on-kill 效果

不需要修改现有技能逻辑，完全解耦。

### 8.2 Boss 与现有刷新系统的关系

Boss 活跃期间：
- `handleSpawning` 中增加 `isBossActive` 检查，降低刷新频率
- 但不清空已有敌人，Boss 战期间小怪仍在场（增加压迫感）

### 8.3 随机性的"可控"

所有随机池都带权重，可以：
- 前 2 分钟锁定某些危险词缀（如不朽）
- 随游戏时间逐步解锁更多词缀
- Boss 词缀单独配置（如 Boss 不会带分裂，因为它本身就是单体挑战）

---

## 九、总结

这个方案的核心思路是 **"渐进式随机"**：不是一开始就扔所有东西给玩家，而是随游戏进程逐步扩大随机池。四个 Layer 各司其职：

| Layer | 体验价值 | 实现成本 |
|-------|----------|----------|
| 属性浮动 + 新敌人 | 缓解重复感 | 低 |
| **精英词缀** ⭐ | 每场遭遇都不同，核心 Rogue 体验 | 中 |
| 随机事件 | 探索乐趣，风险-回报抉择 | 中 |
| Boss 波次 | 节奏高潮，成就感 | 高 |

**建议先从 Phase 1+2 开始**——属性浮动和 5 个核心词缀，这就能让游戏发生质变。有了词缀系统的基础后，Boss 的部分 AI（阶段切换、特殊弹幕）也可以复用词缀的思路。

需要我针对某个具体 Phase 展开详细代码实现计划吗？